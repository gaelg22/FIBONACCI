def Fibo(n):
    fib = [0] *100
    fib [0] = 1
    fib [1] = 1
    for i in range (2, n+1):
        fib[i] = fib[i-2] + fib[i-1]
    print("El numero en la posicion de la serie es: ", fib[n])

n=int(input("\nIngrese la posicion del numero que deseas encontrar : "))
Fibo(n)
    
